# rename to 'config' (no .txt ending)
# place in your .reticulum folder located in your username folder


[reticulum]
  enable_transport = False
  share_instance = Yes
  instance_name = default

discover_interfaces = yes
autoconnect_discovered_interfaces = 0

[logging]
  loglevel = 4

[interfaces]

  [[Default Interface]]
    type = AutoInterface
    enabled = Yes

    # Be part of smaller network
    group_id = KHMesh
    discovery_scope = global

    # Enable your node to be discovered by others
    discoverable = Yes 
    # add your details, optional
    # discovery_name = 

  # extra interface to make our particular internet work?
  [[LAN UDP]]
    type = UDPInterface
    enabled = Yes
    listen_ip = 0.0.0.0
    listen_port = 4242
    forward_ip = 192.168.2.255
    forward_port = 4242

  [[RNode LoRa Interface]]
    #type = RNodeInterface

    # Enable interface if you want use it!
    enabled = no 

    # Serial port for the device, check that it is the correct port, could also be: /dev/ca.usbmodem101, or /dev/ttyUSB0
    port = /dev/tty.usbmodem101 

    # Check that this matches your peers
    frequency = 869525000 

    # Check that this matches your peers
    # bandwidth = 125000

    # does not need to match, higher > further, but more conflicts and power use
    txpower = 7

    # Check that this matches your peers
    spreadingfactor = 8

    # does not need to match, error correction, more = slower data exchange but better reliability
    codingrate = 5

  # See reticulum manual for more explanations and options
  